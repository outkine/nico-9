service: event0-portal

provider:
  name: aws
  runtime: nodejs8.10
  stage: dev
  region: us-east-1

package:
  exclude:
    - ./**
  include:
    - dist/**

functions:
  app:
    handler: dist/server.handler
    events:
      - http:
          path: /
          method: ANY


plugins:
  # - serverless-webpack
  # - serverless-dynamodb-local
  - serverless-offline

custom:
  # webpackIncludeModules: true
  # serverless-offline:
    # babelOptions:
    #   presets: ["env", "stage-0"]
  # dynamodb:
  #   start:
  #     migrate: true
